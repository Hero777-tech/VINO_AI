import React from 'react'

const TransformationsPage = ({ params: {} }) => { //pending part. id needs to update
  return (
    <div>TransformationsPage</div>
  )
}

export default TransformationsPage

//We will be using dynamic routes as we need page.com/transformaers/[id]
//support link - https://nextjs.org/docs/pages/building-your-application/routing/dynamic-routes